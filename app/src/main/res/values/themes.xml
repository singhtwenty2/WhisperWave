<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.IMessage" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/white</item>
    </style>

    <style name="Theme.IMessage" parent="Base.Theme.IMessage" />
    <!-- Light Theme -->
    <style name="AppTheme.Light" parent="Base.Theme.IMessage">
        <!-- Define light theme attributes here -->
        <item name="colorPrimary">@color/white</item>
        <!-- Other attributes -->
    </style>
</resources>